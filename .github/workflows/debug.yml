name: Manual Debug Script

on:
  workflow_dispatch:

jobs:
  run-debug-script:
    name: Run Bash Script
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Debug Script
        run: .github/scripts/debug.sh
